import SwiftUI

// 🌿 Wedding color palette
extension Color {

    static let wildflowerCream = Color(hex: "#F5F1E8") // tło
    static let meadowGreen     = Color(hex: "#6B8E6E") // główna zieleń
    static let sageGreen       = Color(hex: "#9CAF88") // akcenty
    static let blushRose       = Color(hex: "#E8C1C8") // delikatny róż
    static let earthBrown      = Color(hex: "#7A6A5E") // tekst / kontrast
}
